# Robust Responses Guidelines
- Validate inputs (null/empty/type)
- Deterministic structure: title, summary, details, references
- Defensive defaults and timeouts
- Error taxonomy: user-error vs system-error
- Telemetry: log structured events (success, fallback, error)
- Test matrix: happy path, edge cases, timeouts, partial data
